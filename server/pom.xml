<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                      http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- Project Metadata. -->
    <groupId>com.amee</groupId>
    <artifactId>server</artifactId>
    <packaging>pom</packaging>
    <version>${amee.version}</version>
    <name>AMEE Platform</name>

    <!-- Project Properties. -->
    <properties>
        <amee.version>2.1</amee.version>
        <spring.version>2.5.6</spring.version>
        <restlet.version>1.1.3</restlet.version>
        <org.json.version>2.0</org.json.version>
        <joda-time.version>1.6</joda-time.version>
        <hibernate-core.version>3.3.0.SP1</hibernate-core.version>
        <hibernate-entitymanager.version>3.4.0.GA</hibernate-entitymanager.version>
        <jetty.version>6.1.9</jetty.version>
        <ehcache.version>1.6.0-beta4</ehcache.version>
        <!--<ehcache.version>1.5.0</ehcache.version>-->
        <commons-dbcp.version>1.2.2</commons-dbcp.version>
    </properties>

    <!-- AMEE Modules. -->
    <modules>
        <module>amee-admin</module>
        <module>amee-calculation</module>
        <module>amee-core</module>
        <module>amee-domain</module>
        <module>amee-engine</module>
        <module>amee-restlet</module>
        <module>amee-service</module>
    </modules>

    <!-- Build Plugins. -->
    <build>

        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <configuration>
                        <source>1.6</source>
                        <target>1.6</target>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>

        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <executions>
                    <execution>
                        <id>copy-dependencies</id>
                        <phase>package</phase>
                        <goals>
                            <goal>copy-dependencies</goal>
                        </goals>
                        <configuration>
                            <excludeGroupIds>com.amee</excludeGroupIds>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <!-- Maven Repositories. -->
    <repositories>
        <repository>
            <id>maven-restlet</id>
            <name>Public online Restlet repository</name>
            <url>http://maven.restlet.org</url>
        </repository>
        <repository>
            <id>apache-incubating</id>
            <name>Apache Incubating Repository</name>
            <url>http://people.apache.org/repo/m2-incubating-repository/</url>
        </repository>
    </repositories>

    <!-- Project Dependencies. -->
    <dependencies>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-beans</artifactId>
            <version>${spring.version}</version>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-context</artifactId>
            <version>${spring.version}</version>
        </dependency>
        <dependency>
            <groupId>log4j</groupId>
            <artifactId>log4j</artifactId>
            <version>1.2.14</version>
        </dependency>
        <dependency>
            <groupId>commons-logging</groupId>
            <artifactId>commons-logging</artifactId>
            <version>1.1</version>
        </dependency>

        <!-- Test Dependencies. -->
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>4.5</version>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <!-- Site & Reporting. -->
    <reporting>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-project-info-reports-plugin</artifactId>
                <configuration>
                    <webAccessUrl>https://github.com/sambogoat/amee/tree/master</webAccessUrl>
                    <anonymousConnection>scm:git@github.com:sambogoat/amee.git</anonymousConnection>
                    <developerConnection>scm:git@github.com:sambogoat/amee.git</developerConnection>
                </configuration>
            </plugin>
            <plugin>
                <artifactId>maven-javadoc-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>jxr-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-pmd-plugin</artifactId>
                <configuration>
                    <targetjdk>1.6</targetjdk>
                    <rulesets>
                        <!-- Add other rulesets. -->
                        <ruleset>/rulesets/basic.xml</ruleset>
                        <!--<ruleset>/rulesets/controversial.xml</ruleset>-->
                    </rulesets>
                    <format>xml</format>
                    <linkXref>true</linkXref>
                    <sourceEncoding>utf-8</sourceEncoding>
                    <minimumTokens>100</minimumTokens>
                </configuration>
            </plugin>
        </plugins>
    </reporting>

    <scm>
        <connection>scm:svn:http://svn.example.org/repo/project/trunk/site</connection>
        <developerConnection>scm:git@github.com:sambogoat/amee.git</developerConnection>
        <url>https://github.com/sambogoat/amee/tree/master</url>
    </scm>

    <issueManagement>
        <system>Trac</system>
        <url>http://my.amee.com/developers/roadmap</url>
    </issueManagement>

    <ciManagement>
        <system>Continuum</system>
        <url>http://localhost:81/continuum</url>
        <notifiers>
            <notifier>
                <type>mail</type>
                <address>duke@wakaleo.com</address>
            </notifier>
        </notifiers>
    </ciManagement>


</project>
